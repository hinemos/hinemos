language: java
jdk:
# - openjdk7 # Execution failed for task ':manager:compileJava'. Could not target platform: 'Java SE 8' using tool chain: 'JDK 7 (1.7)'.
- openjdk8

# Deal with multi-projects repo
matrix:
  include:
  - name: "Build common"
    env: SUB_PROJECT=common
  script: gradle :$SUB_PROJECT:build
